import React from 'react';
import AppNavigatorMobile from './AppNavigator.mobile';

const AppNavigator = (props: any) => {
    return <AppNavigatorMobile {...props} />;
};

export default AppNavigator;
