import React from 'react';
import LandingScreenMobile from './LandingScreen.mobile';

const LandingScreen = (props: any) => {
    return <LandingScreenMobile {...props} />;
};

export default LandingScreen;
