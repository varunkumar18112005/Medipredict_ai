# Server
server.port=8085
server.servlet.context-path=/api/v1

# Database (PostgreSQL)
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/medipredict_db}
spring.datasource.username=${DB_USERNAME:medipredict}
spring.datasource.password=${DB_PASSWORD:medipredict123}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.time_zone=UTC

# JWT Configuration
jwt.secret=mysupersecretkey12345678901234567890
app.jwt.secret=${JWT_SECRET:MediPredictAISecretKey2024VeryLongSecretKeyForHS512AlgorithmSecurity}
app.jwt.expiration=86400000
app.jwt.refresh-expiration=604800000

# Mail (Gmail SMTP Port 587 TLS)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=varunbojjireddy98@gmail.com
spring.mail.password=vhpzpszcgarwwcqw
spring.mail.protocol=smtp
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=10000
spring.mail.properties.mail.smtp.timeout=10000
spring.mail.properties.mail.smtp.writetimeout=10000

# File Upload
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
app.upload.dir=${UPLOAD_DIR:./uploads}

# ML Microservice URL
app.ml.service.url=${ML_SERVICE_URL:http://localhost:8000}

# CORS
app.cors.allowed-origins=${CORS_ORIGINS:http://localhost:*,http://127.0.0.1:*,http://localhost:3000,http://localhost:8081}

# Logging
logging.level.com.medipredict=INFO
logging.level.org.springframework.security=WARN

# Swagger/OpenAPI
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.operationsSorter=method

# External API Keys (Google Places & OpenRouteService)
geoapify.api.key=798e449799254c13958f7e868a33dd39
openrouteservice.api-key=eyJvcmciOiI1YjNjZTM1OTc4NTExMTAwMDFjZjYyNDgiLCJpZCI6IjM0ZGZjYTcxYmUzNjQ1MWU5MzFiYzU4NzQzOTYyODRlIiwiaCI6Im11cm11cjY0In0=

# Python ML Microservice URL (Localhost:8000 for local dev, ml-service:8000 for Docker)
app.ml.service.url=${APP_ML_SERVICE_URL:http://localhost:8000}