# Server
server.port=${SERVER_PORT:8085}
server.servlet.context-path=/api/v1

# Database (PostgreSQL)
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://dpg-da1scnvlk1mc73a8cdfg-a:5432/medipredict_db_adxf}
spring.datasource.username=${DB_USERNAME:medipredict}
spring.datasource.password=${DB_PASSWORD:eqomNcQX46LSjyQbvtEkaRDmz7kftcEs}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.time_zone=UTC

# JWT Configuration
jwt.secret=${JWT_SECRET:mysupersecretkey12345678901234567890}
app.jwt.secret=${JWT_SECRET:MediPredictAISecretKey2024VeryLongSecretKeyForHS512AlgorithmSecurity}
app.jwt.expiration=86400000
app.jwt.refresh-expiration=604800000

# Mail (Gmail SMTP Port 587 TLS)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${SPRING_MAIL_USERNAME:}
spring.mail.password=${SPRING_MAIL_PASSWORD:}
spring.mail.protocol=smtp
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=10000
spring.mail.properties.mail.smtp.timeout=10000
spring.mail.properties.mail.smtp.writetimeout=10000

# File Upload
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
app.upload.dir=${UPLOAD_DIR:./uploads}

# ML Microservice URL
app.ml.service.url=${APP_ML_SERVICE_URL:${ML_SERVICE_URL:https://medipredict-ml.onrender.com}}

# CORS
app.cors.allowed-origins=${CORS_ORIGINS:*}

# Logging
logging.level.com.medipredict=INFO
logging.level.org.springframework.security=WARN

# Swagger/OpenAPI
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.operationsSorter=method

# External API Keys (Google Places & OpenRouteService)
geoapify.api.key=${GEOAPIFY_API_KEY:798e449799254c13958f7e868a33dd39}
openrouteservice.api-key=${OPENROUTESERVICE_API_KEY:eyJvcmciOiI1YjNjZTM1OTc4NTExMTAwMDFjZjYyNDgiLCJpZCI6IjM0ZGZjYTcxYmUzNjQ1MWU5MzFiYzU4NzQzOTYyODRlIiwiaCI6Im11cm11cjY0In0=}